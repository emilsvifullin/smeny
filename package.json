{
  "name": "shift-register",
  "version": "6.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node --check src/config.js && node --check src/domain.js && node --check src/storage.js && node --check src/app.js && node --check sw.js && node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
